        var b = word / div;
            ret = b;
            ret = (ret * 64) | (b & 0x3F);
        playerProfit[rngId] = msg.value * 100 / (safeSub(rollUnder, 1)) * edge / houseEdgeDivisor - msg.value;
